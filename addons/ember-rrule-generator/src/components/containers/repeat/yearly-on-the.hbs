{{#let
  (hash
    Which=(component
      this.YearlyOnTheWhich
      id=(concat @id "-which")
      value=@onThe.which
      handleChange=@handleChange
      name=(concat @name ".which")
      options=this.whichs
      isActive=@isActive
    )
    Month=(component
      this.YearlyOnTheMonth
      id=(concat @id "-month")
      value=@onThe.month
      handleChange=@handleChange
      name=(concat @name ".month")
      options=this.months
      isActive=@isActive
    )
    Day=(component
      this.YearlyOnTheDay
      id=(concat @id "-day")
      value=@onThe.day
      handleChange=@handleChange
      name=(concat @name ".day")
      options=this.days
      isActive=@isActive
    )
  )
  as |YearlyOnThe|
}}
  {{#if (has-block)}}
    {{yield YearlyOnThe}}
  {{else}}
    {{#let (component @view) as |YearlyOnTheComponent|}}
      <YearlyOnTheComponent @yearlyOnTheApi={{YearlyOnThe}} ...attributes />
    {{/let}}
  {{/if}}

{{/let}}