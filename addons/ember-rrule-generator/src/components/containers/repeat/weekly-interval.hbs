{{#if (has-block)}}
  {{yield (hash id=@id name=@name value=@value handleChange=this.handleChange translations=@translations)}}
{{else}}
  {{#if @view}}
    {{#let (component @view) as |WeeklyIntervalComponent|}}
      <WeeklyIntervalComponent
        @id={{@id}}
        id={{@id}}
        name={{@name}}
        @name={{@name}}
        @value={{@value}}
        @handleChange={{this.handleChange}}
        @translations={{@translations}}
        ...attributes
      />
    {{/let}}
  {{/if}}
{{/if}}