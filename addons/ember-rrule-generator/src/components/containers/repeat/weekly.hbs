{{#let
  (hash
    Interval=(component
      this.WeeklyInterval
      id=(concat @id "-interval")
      value=@weekly.interval
      handleChange=@handleChange
      name=(concat @name ".interval")
    )
    Days=(component
      this.WeeklyDays
      id=(concat @id "-days")
      days=this.days
      value=@weekly.days
      handleChange=@handleChange
      name=(concat @name ".days")
    )
  )
  as |Weekly|
}}
  {{#if (has-block)}}
    {{yield Weekly}}
  {{else}}
    {{#let (component @view) as |WeeklyComponent|}}
      <WeeklyComponent @weeklyApi={{Weekly}} ...attributes/>
    {{/let}}
  {{/if}}
{{/let}}