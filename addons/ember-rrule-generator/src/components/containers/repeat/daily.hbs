{{#let
  (hash
    Interval=(component
      this.DailyInterval
      id=(concat @id "-interval")
      value=@daily.interval
      handleChange=@handleChange
      name=(concat @name ".interval")
    )
  )
  as |Daily|
}}
  {{#if (has-block)}}
    {{yield Daily}}
  {{else}}
    {{#let (component @view) as |DailyComponent|}}
      <DailyComponent @dailyApi={{Daily}} />
    {{/let}}
  {{/if}}
{{/let}}