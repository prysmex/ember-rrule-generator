{{#if (has-block)}}
  {{yield
    (hash
      handleChange=this.handleChange
      onDate=@onDate
      value=@onDate.date
      id=@id
      name=@name
      translations=@translations
      labels=@labels
    )
  }}
{{else}}
  {{#if @view}}
    {{#let (component @view) as |OnDateComponent|}}
      <OnDateComponent
        @id={{@id}}
        id={{@id}}
        @name={{@name}}
        name={{@name}}
        @value={{@onDate.date}}
        @handleChange={{this.handleChange}}
        @translations={{@translations}}
        @labels={{@labels}}
        ...attributes
      />
    {{/let}}
  {{/if}}
{{/if}}