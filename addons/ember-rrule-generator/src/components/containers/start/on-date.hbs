{{#if (has-block)}}
  {{yield
    (hash
      handleChange=@handleChange
      dateFormatting=this.dateFormat
      onDate=@onDate
      value=@onDate.date
      name=(concat @name ".date")
      id=@id
      translations=@translations
      labels=@labels
      isDisabled=@isDisabled
      name=@name
    )
  }}
{{else}}
  {{#if @view}}
    {{#let (component @view) as |OnDateComponent|}}
      <OnDateComponent
        @id={{@id}}
        id={{@id}}
        @name={{concat @name ".date"}}
        name={{concat @name ".date"}}
        @value={{@onDate.date}}
        @onDate={{@onDate}}
        @dateFormatting={{this.dateFormat}}
        @handleChange={{@handleChange}}
        @translations={{@translations}}
        @labels={{@labels}}
        @isDisabled={{@isDisabled}}
        ...attributes
      />
    {{/let}}
  {{/if}}
{{/if}}